[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "nodejs_20"]

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction",
    "npm install && npm run build"
]

[phases.build]
# لا تشغل config:cache أثناء البناء لأنه يحتاج متغيرات Railway غير المتاحة بعد
# سيتم تشغيلها في start command بدلاً من ذلك
cmds = [
    "php artisan storage:link"
]

[start]
cmd = "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"